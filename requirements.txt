# LLM orchestration
langchain==0.3.27
langchain-core==0.3.76
langchain-text-splitters==0.3.9
langgraph==0.6.7

# LLM providers (only the ones you configure a key for are actually used)
langchain-openai==0.3.33
langchain-anthropic==0.3.21
langchain-mistralai==0.2.11
langchain-huggingface==0.3.1

# Embeddings / vector store
langchain-chroma==0.2.6
chromadb==1.1.1
sentence-transformers==5.1.1
# Pinned: neither chromadb nor sentence-transformers pins numpy itself, so
# pip is free to resolve numpy 2.x — which some platform builds of the
# torch version they pull in are not ABI-compatible with, surfacing as
# "Failed to initialize NumPy: _ARRAY_API not found" / "RuntimeError:
# Numpy is not available" the first time an embedding is computed. numpy<2
# is compatible with every currently-pinned torch build.
numpy<2

# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6

# Scraper / ingestion
requests==2.32.3
pdfplumber==0.11.4
# Pinned: pypdfium2 (pdfplumber's PDF engine) stopped shipping broadly
# compatible wheels after 4.30.0 — newer releases require building from
# source on some platform/Python combos, which pulls in a GitHub CLI
# attestation-verification step (needs gh >= 2.47.0). 4.30.0 still ships a
# prebuilt wheel for macOS 10.13+ (x86_64) and every other common target,
# so pin it to avoid that build step entirely.
pypdfium2==4.30.0
selenium==4.27.1
tenacity==9.0.0

# Config
python-dotenv==1.0.1

# Tests
pytest==8.3.4
pytest-mock==3.14.0
pytest-asyncio==0.25.2
httpx==0.28.1
